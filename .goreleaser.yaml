# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: geocheck

before:
  hooks:
    - go mod tidy

builds:
  - id: geocheck
    main: ./cmd/geocheck
    binary: geocheck
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    flags: [-trimpath]
    mod_timestamp: "{{ .CommitTimestamp }}"
    ldflags:
      - -s -w
      - -X github.com/remnawave/geocheck/internal/version.Version={{ .Version }}
      - -X github.com/remnawave/geocheck/internal/version.Commit={{ .FullCommit }}
      - -X github.com/remnawave/geocheck/internal/version.Date={{ .CommitDate }}

universal_binaries:
  - id: geocheck
    ids: [geocheck]
    replace: true

archives:
  - id: default
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Os }}_{{ .Arch }}
    files:
      - LICENSE
      - README.md

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github

  format: "[`{{ slice .SHA 0 7 }}`](https://github.com/wakeupmetha/geocheck/commit/{{ .SHA }}) {{ .Message }}{{ with .Logins }} – {{ . | englishJoin }}{{ end }}"

  groups:
    - title: Features
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 0
    - title: Fixes
      regexp: '^.*?fix(\(.+\))??!?:.+$'
      order: 1
    - title: Other
      order: 99
  filters:
    exclude:
      - '^.*?docs(\(.+\))??!?:'
      - '^.*?test(\(.+\))??!?:'
      - '^.*?chore(\(.+\))??!?:'
      - '^.*?build(\(.+\))??!?:'
      - '^.*?ci(\(.+\))??!?:'
      - Merge pull request
      - Merge branch

release:
  footer: |
    ## Running it

    **One command, nothing installed:**

    ```sh
    curl -fsSL https://raw.githubusercontent.com/wakeupmetha/geocheck/main/scripts/geocheck.sh | sh
    curl -fsSL https://raw.githubusercontent.com/wakeupmetha/geocheck/main/scripts/geocheck.sh | sh -s -- -4 --detail       # with flags
    curl -fsSL https://raw.githubusercontent.com/wakeupmetha/geocheck/main/scripts/geocheck.sh | sh -s -- --runtime binary  # pin the runtime
    ```

    The launcher uses docker, else podman, else it downloads the binary below –
    verifying its checksum – into a temporary directory. It always runs the
    current release, installs nothing, and tidies up after itself: an image it
    pulled is removed again, and a downloaded binary is deleted. An image you
    already had is left alone. Set `GEOCHECK_KEEP_IMAGE=1` to keep the pulled
    image so later runs start instantly.

    **Straight from the registry:**

    ```sh
    docker pull ghcr.io/wakeupmetha/geocheck:ing && docker run --rm -it --network host ghcr.io/wakeupmetha/geocheck:ing
    ```

    Under podman add `--cap-add=NET_RAW`: docker grants that capability by
    default and podman does not, and without it the path trace degrades to
    latency only.

    **As a binary** – download the archive for your platform from the assets
    below, or build it from a checkout. `go install` is not offered: this is a
    fork, and the module is still named after upstream, so installing by module
    path would fetch upstream's build without the Twitch checks.

    ```sh
    git clone https://github.com/wakeupmetha/geocheck && cd geocheck && make build
    ```

    Hop-by-hop tracing needs a raw socket. The container image already carries
    the capability; for a plain binary, grant it once with
    `sudo setcap cap_net_raw+p ./geocheck` on Linux, or run with `sudo` on
    macOS. Without it geocheck still measures destination latency over TCP and
    says so.
